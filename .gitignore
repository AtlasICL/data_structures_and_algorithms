# Ignore everything by default
*

# Do not ignore directories so Git can search inside them
!*/

# Unignore any file that has an extension (recursively)
!**/*.*

# Also ignore any other unnecessary files
.vscode
*.out
